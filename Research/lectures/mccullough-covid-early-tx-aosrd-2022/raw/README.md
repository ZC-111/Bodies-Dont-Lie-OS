# Raw

No Otter transcript or audio linked yet for this packet.

If a matching recording/export is found, drop:

- `*.txt` (Otter transcript)
- `*.mp3` / `*.m4a` (audio)

Then set `packet.json` → `otter` and `sync` to `matched` (if slides remain) or `transcript_only`.
