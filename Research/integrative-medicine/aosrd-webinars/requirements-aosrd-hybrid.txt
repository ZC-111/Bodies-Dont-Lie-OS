# Optional deps for AOSRD hybrid lecture processor only.
# Do NOT merge into the default acupuncture requirements.txt / cloud install
# unless you intend every agent VM to pull Whisper + yt-dlp.
#
#   pip3 install -r Research/integrative-medicine/aosrd-webinars/requirements-aosrd-hybrid.txt
#
# Also needs system packages: ffmpeg, and poppler (for pdf2image).

requests>=2.31.0
beautifulsoup4>=4.12.0
yt-dlp>=2024.8.0
faster-whisper>=1.0.0
pdf2image>=1.17.0
Pillow>=10.0.0
